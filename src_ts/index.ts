/**
 * Default export is IntervalResizer. If you want to use both the component and
 * the types in your typescript import as follows:
 * import IntervalResizer, { IIntervalResizerProps } from 'interval-resizer';
 */
export default './IntervalResizer';
export * from './IntervalResizer.types';