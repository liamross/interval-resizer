{
  "name": "interval-resizer",
  "version": "2.1.1",
  "description": "A React resizer which detects the height of its internals, then expands them to the nearest multiple of a pre-defined interval.",
  "main": "./build/IntervalResizer.js",
  "scripts": {
    "build": "babel ./src --out-dir ./build --source-maps && npm run buildDemo",
    "buildDemo": "babel ./src --out-dir ./demo/src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/liamross/interval-resizer.git"
  },
  "author": "Liam Ross",
  "license": "MIT",
  "peerDependencies": {
    "react": ">=15.6.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-react-jsx": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  },
  "dependencies": {
    "prop-types": ">=15.6.0"
  }
}
